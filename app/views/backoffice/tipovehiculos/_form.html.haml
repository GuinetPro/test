= form_for ["backoffice",@tipovehiculo] do |f|
  - if @tipovehiculo.errors.any?
    .alert.alert-warning
      %h2
        = pluralize(@tipovehiculo.errors.count, "error")
        Al Intentar Guardar Tipo Vehiculo:
      %ul
        - @tipovehiculo.errors.full_messages.each do |message|
          %li= message
  .form-group
    = f.label :nombre
    %br/
    = f.text_field :nombre ,class: "form-control"
  .form-group
    = f.submit "Guardar" ,class: "btn btn-success btn-lg btn-block"
