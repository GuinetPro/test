<%= form_for ["backoffice",@vigencia] do |f| %>
  <% if @vigencia.errors.any? %>
    <div class="alert alert-warning">
      <h2><%= pluralize(@vigencia.errors.count, "error") %> Al Intentar Guardar Vigencia :</h2>

      <ul>
      <% @vigencia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :fecha_desde %><br>
    <%= f.text_field :fecha_desde ,class: "form-control date" %>
  </div>
  <div class="form-group">
    <%= f.label :fecha_hasta %><br>
    <%= f.text_field :fecha_hasta ,class: "form-control date" %>
  </div>

  <div class="form-group">
      <div class="checkbox">
        <label>
          <%= f.check_box :activado %> Activado
        </label>
      </div>
  </div>


  <div class="form-group">

   <%= f.select(:tipovigencia_id, @tipovigencia.all.collect {|a| [a.nombre, a.id]},{multiple:  false,include_blank: 'Elige una Vigencia'}, { :class => 'form-control' })  %>

  </div>

  <div class="form-group">

  <%= f.select(:campana_id, @campana.all.collect {|a| [a.nombre, a.id]} ,{multiple:  false,include_blank: 'Elige una Campaña'}, { :class => 'form-control' })  %>

  </div>
  <div class="form-group">
    <%= f.submit "Guardar" ,class: "btn btn-success btn-lg btn-block" %>
  </div>
<% end %>


<script type="text/javascript">
  
      $('.date').datepicker({
        
        language: "es",
        
        calendarWeeks: true,
        
        autoclose: true,
        
        todayHighlight: true,

        format: "dd-mm-yyyy"
    });
</script>